# Frontend (React + SCSS + Leaflet)

## Install
```bash
npm install
```

## Run (dev)
```bash
npm run dev
```

The UI expects the backend at `http://localhost:8000` by default.

Tip: use the datasets from `../data/`:
- MDBB_parts_usage.json
- Issues_train.json
- sample_leads.json
